{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "The TSUZU main window can call only the command permitted by build.rs.",
  "windows": ["main"],
  "permissions": ["allow-runtime-health"]
}
