{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "The TSUZU main window can check runtime health, persist app-local drafts, and invoke the fixed Core ingress command.",
  "windows": ["main"],
  "permissions": [
    "allow-runtime-health",
    "allow-process-desktop-ingress",
    "fs:default",
    "fs:allow-app-read-recursive",
    "fs:allow-app-write-recursive"
  ]
}
