{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "The TSUZU main window can check runtime health and persist Canonical SOURCE only in its app-local directory.",
  "windows": ["main"],
  "permissions": [
    "allow-runtime-health",
    "fs:default",
    "fs:allow-app-read-recursive",
    "fs:allow-app-write-recursive"
  ]
}
